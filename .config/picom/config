# Backend
backend = "glx"
vsync = true
xrender-sync-fence = true

# Shadows
shadow = false;
shadow-radius = 5;
shadow-offset-x = -5;
shadow-offset-y = -5;
shadow-opacity = 0.2;
shadow-ignore-shaped = false;
shadow-exclude = [];

# Opacity
# opacity-rule = ["90:class_g = 'kitty'"]

# Blur
blur-method = "gaussian";
blur-strength = 2;

# Fading
fading = true;
fade-in-step = 0.07;
fade-out-step = 0.07;
